import React from 'react';

const SearchBarComponent = () => {
    return (
        <div>
            <input type="text"/>
            <button>APPLY</button>
        </div>
    );
};

export default SearchBarComponent;